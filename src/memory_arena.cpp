// memory_arena.cpp
// MemoryArena<T,N> is fully defined in memory_arena.hpp (it is a class
// template, so all code lives in the header).  This translation unit exists
// only to satisfy the CMake target dependency graph.
#include "memory_arena.hpp"
